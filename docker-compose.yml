version: '2'

services:

  wordpress:
    image: wordpress
    ports:
      - 8082:80
    env_file:
      - variables.env
    volumes:
      - ./data/wordpress:/var/www/html


  mysql:
    image: mariadb
    env_file:
      - variables.env
    volumes:
      - ./data/mysql:/var/lib/mysql


