# PHP in the official WordPress image runs through Apache prefork. Keep the worker
# pool below its memory budget so a traffic burst queues requests rather than causing OOM kills.
StartServers             3
MinSpareServers          3
MaxSpareServers          6
MaxRequestWorkers       12
MaxConnectionsPerChild 500
